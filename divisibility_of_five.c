// divisibility test of five
#include<stdio.h>
int main () 
{
    int a;
    printf(" Enter a value: ");
    scanf("%d",&a);
    if (a%5==0) {
        printf("a value is divisible by 5"); }
        else {printf ("a is not divisible by 5");
        }
        return 0;
    }
